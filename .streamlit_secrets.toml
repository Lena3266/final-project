# Streamlit Secrets Configuration
# Save this to Streamlit Cloud "Secrets" management page OR locally at ~/.streamlit/secrets.toml

# Backend Configuration
backend_url = "http://localhost:8000"

# Optional: API Keys or environment-specific settings
# api_key = "your-key-here"
# debug_mode = false

# For Streamlit Cloud deployment:
# 1. Go to your app settings (three dots menu)
# 2. Click "Secrets" 
# 3. Copy and paste the TOML format below (without comments)
# 4. Update values as needed for your deployment

# Example full config for production:
# backend_url = "https://your-production-backend.com"
# api_key = "your-secret-api-key"
# model_name = "default"
